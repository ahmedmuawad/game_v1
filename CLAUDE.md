# LIVI — ذاكرة المشروع

لعبة محاكاة حياة وقصة تفاعلية لفتيات 11–16، عربي/إنجليزي.
**اللغة:** كل الوثائق والردود والتعليقات بالعربية. الكود بالإنجليزية.

---

## أوامر أساسية

```bash
npm run dev              # خادم التطوير
npm run dev -- --host    # للتجربة من الموبايل على نفس الشبكة
npm run build            # بناء إنتاجي
npm run typecheck        # التطبيق + الاختبارات
npm test                 # 140 اختبار
npm run story:validate   # تدقيق كل فصول القصة بلا متصفح
npm run bundle           # نسخة ملف واحد → dist-single/livi.html
npm run shot             # لقطات آلية (يحتاج vite preview شغّال)
npm run story:check      # تشغيل آلي يلعب الفصل الأول

npx cap add android      # أول مرة
npm run cap:android      # بناء + مزامنة + فتح Android Studio
npm run android:apk      # APK مباشرة
```

## أصول الشخصيات (Python 3.11 + bpy)

```bash
python3.11 -m venv .venv && source .venv/bin/activate
pip install "bpy==4.2.23" numpy pillow
bash tools/avatar/assets/fetch.sh        # الشبكة الأساسية (CC0، غير مخزّنة في git)
npm run export:avatar:full               # ~15 دقيقة، 203 طبقة
```

---

## قواعد معمارية غير قابلة للتفاوض

1. **صفر نصوص في المكوّنات.** كل نص يواجه اللاعب له مفتاح في
   `src/i18n/strings.ts` أو حقل `{ar, en}` في المحتوى.
2. **خصائص CSS المنطقية حصريًا** (`margin-inline-start` لا `margin-left`).
   الاستثناء يحتاج تعليقًا يشرح السبب.
3. **المنطق في `systems/` بلا React ولا وصول للمخزن.** الشاشات تقرأ من
   المخزن وتستدعي أفعاله، ولا تحسب منطق لعب.
4. **المحتوى بيانات لا كود.** إضافة فصل أو قطعة أزياء يجب ألا تحتاج
   بناءً جديدًا للتطبيق.
5. **لا `eval` في تقييم شروط القصة.** المحتوى مصمَّم للتحميل عن بُعد.
6. **خط الإنتاج لا يُصدّر شخصية بلا ملابس أبدًا** — الشبكة الأساسية
   محايدة وعارية. أي مصغّرة تُركَّب فوق إطلالة.
7. **قيم التوازن في `systems/config.ts` فقط**، لا مبعثرة في الكود.

## محرّمات المنتج

لا صناديق غنائم ولا أي عشوائية مدفوعة · لا بيع اختيارات سردية ·
لا بيع استعادة السلسلة · لا شات ولا نص حر بين اللاعبين ·
لا بيع الهوية (بشرة/شعر/عيون مجانية دائمًا) ·
إعلانات غير مخصّصة فقط (`npa: true`) بسقف يومي صارم.

السبب في `docs/DECISIONS.md#D-002` — الجمهور كله قاصرون وتعديلات
COPPA نافذة من أبريل 2026.

---

## الحالة الحالية

| المرحلة | الحالة |
|---|---|
| وثائق المنتج (11 مستندًا) | ✅ |
| نظام التصميم + i18n/RTL | ✅ |
| خط إنتاج الشخصيات 3D → طبقات | ✅ 203 طبقة مصدَّرة |
| محرك القصص | ✅ + خلفيات أماكن + تسلسل مواسم |
| المحتوى | ✅ **موسمان · 12 فصل · 8 شخصيات** |
| شاشة الستايل | ✅ |
| شاشتا القصة | ✅ |
| واجهة الحلقة اليومية | ✅ سلسلة + هدية + مهام |
| المتجر | ✅ مع معاينة القطعة على الأفاتار |
| الغرفة | ✅ 6 حوائط · 5 أرضيات · 17 قطعة · إضاءة · الأفاتار جوّاها |
| الألعاب المصغّرة | ✅ خمس ألعاب |
| الأونبوردنج | ✅ خمس خطوات |
| شاشة الإعدادات | ✅ اللغة بقت قابلة للتبديل على الموبايل |
| التغليف الأصلي | ✅ `com.stop4web.livi` · APK debug يُبنى |
| الحساب والمزامنة | ⏳ المنطق والاختبارات جاهزة · **موقوف على إعدادات تطبيق ويب من Firebase** |
| بورتريهات الشخصيات الجديدة | ⏳ محتاج بايثون 3.11 + bpy (مش متاح على الجهاز ده) |

**مفيش شاشة «قريبًا» في التطبيق.** كل تبويب في شريط التنقل بيوصّل
لشاشة شغّالة.

**أولويات الإصلاح مرتّبة في `docs/QUALITY_REVIEW.md` §7.**
الباقي الآن: بورتريهات الشخصيات الجديدة (ليلى، ريم، نادية) تحتاج خط
الإنتاج ثلاثي الأبعاد، وربط الحساب موقوف على إعدادات Firebase.

## البناء المحلي (تحقّق على Windows)

- `JAVA_HOME` لازم يشاور على JDK 17 أو 21. لو الـ`java` في الـPATH
  إصدار 11 هيفشل Gradle — استخدم `C:\Program Files\Android\Android Studio\jbr`.
- إصدار `@capacitor/cli` لازم يطابق الإصدار الرئيسي لـ`core`/`android`.
- للتجربة على موبايل موصول بالـUSB بلا اعتماد على الشبكة:
  `adb reverse tcp:5173 tcp:5173` ثم افتح `http://localhost:5173` في متصفح الموبايل.

---

## خريطة سريعة

```
src/systems/      منطق نقي: story/ · config · energy · daily · progression · analytics · ads
src/features/     الشاشات: StyleScreen · story/ · shop/ · room/ · play/ · settings/ · onboarding/ · daily/
src/content/      manifest.ts — يقرأ الأصول المولَّدة وقت التشغيل
src/state/        types · defaults · store (Zustand + persist)
tools/avatar/     خط الإنتاج: wardrobe.py هو مصدر الحقيقة للكتالوج
tools/qa/         لقطات · تشغيل آلي · نسخة الملف الواحد
public/avatar/    227 صورة + manifest.json
public/story/     الموسمان — s1 و s2
docs/             ابدأ بـ PRODUCT_BLUEPRINT.md ثم DECISIONS.md
```

## أصول خارجية

`tools/avatar/assets/base.obj` — شبكة MakeHuman الأساسية، **CC0 صراحةً**
(استخدام تجاري حر). غير مخزّنة في git لحجمها؛ `fetch.sh` ينزّلها.
